# Configuração pessoal do Claude Code (caminhos absolutos da máquina local)
.claude/settings.local.json

# Área de trabalho do executor de planos (rascunho, não versionado)
.superpowers/

# Front-end
node_modules/
dist/
coverage/
*.local
.env
.env.*
!.env.example

# Playwright (Tarefa 21)
playwright-report/
test-results/
blob-report/
.vscode/settings.json

# Captura de tela feita durante o desenvolvimento para conferir layout.
# Artefato de trabalho, não do projeto: o que documenta desenho mora em
# `design/`, e o que prova comportamento é o e2e.
captura-*.png
